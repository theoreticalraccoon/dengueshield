# Runtime dependencies for the DengueShield Streamlit app.
# Training-only packages (torch, shap, xgboost, duckdb, matplotlib) are deliberately
# excluded - the app loads pre-trained models and pre-computed reports.
streamlit>=1.40
pandas>=2.0
numpy>=1.26
plotly>=5.20
joblib>=1.3
scikit-learn>=1.4
lightgbm>=4.3
pyarrow>=15.0
